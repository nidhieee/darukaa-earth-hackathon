# Backend API

- Code lives in `api/`.
- Framework: FastAPI
- ORM: SQLAlchemy + GeoAlchemy2
- Tools: pre-commit + black for formatting and linting.

To run locally, you will run the FastAPI server from the `api/` directory.
