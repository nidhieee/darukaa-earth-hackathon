# Deployment

- **Backend:** `api/` is deployed on Render.com. The root directory for Render is `api/`.
- **Frontend:** `web/` is deployed on Vercel. The root directory for Vercel is `web/`.
- **Database:** Hosted on Supabase.
