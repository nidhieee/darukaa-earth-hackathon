# Frontend Web

- Code lives in `web/`.
- Framework: React (built with Vite)
- Libraries: Mapbox GL JS, mapbox-gl-draw, Chart.js
- Tools: Husky + lint-staged for pre-commit hooks.

To run locally, you will run the Vite dev server from the `web/` directory.
